services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: oak_api
    environment:
      # App server
      SERVICE_HOST: 0.0.0.0
      SERVICE_PORT: 8000
    ports:
      - "8000:8000"
    restart: unless-stopped
